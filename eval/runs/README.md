# Eval runs

Mỗi lần chạy tạo một file JSON và một file Markdown có cùng timestamp.

Không sửa tay kết quả model. Nếu runner hoặc rubric có lỗi, sửa code rồi chạy một lượt mới; giữ lại lượt cũ để bảo toàn lịch sử.
